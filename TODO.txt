- Стиль линии (пунктир и тп) в LineDrawStyle.
	BasicStroke(float width, int cap, int join, float miter, float[] dash, float dashBegin)

	задает:

  	  толщину пера width в пикселах;
  	  оформление конца линии cap; это одна из констант:
   	     CAP_ROUND — закругленный конец линии;
   	     CAP_SQUARE — квадратный конец линии;
   	     CAP_BUTT — оформление отсутствует; 
  	  способ сопряжения линий join; это одна из констант:
  	      JOIN_ROUND — линии сопрягаются дугой окружности;
    	    JOIN_BEVEL — линии сопрягаются отрезком прямой, перпендикуляр-ным биссектрисе угла между линиями;
  	      JOIN_MITER — линии просто стыкуются; 
  	  расстояние между линиями miter, начиная с которого применяется сопряжение JOIN_MITER;
  	  длину штрихов и промежутков между штрихами — массив dash; элементы массива с четными индексами задают длину штриха в пикселах, элементы с нечетными индексами — длину промежутка; массив перебирается циклически;
  	  индекс dashBegin, начиная с которого перебираются элементы массива
  	  dash. 
	хранить как float[], редактировать в отдельной форме как набор квадратиков в линию: закрашенные белым и черным цветом
	(первые квадраты обязательно черный) черный - рисовать линию, белый не рисовать
- Диалог на шрифт
	http://forum.vingrad.ru/topic-197453.html
- Поддержка нескольких языков для description MapObjectStyle
	Много языков как связка Язык-описание,
	Хранятся в виде списка, список языков отдельно.
- Mapobjectstyleeditor и viewer - выделить общую часть (загрузка из файла и поиск по ид и тегам) как интерфейс
- Панель просмотра объекта в диалоге Объекты на карте
- Сделать в диалоге список не выпадающий с возможностью выделения нескольких,вынести общие контролы из диалога для применения изменений ко всем выделенным
- запоминание размеров и максимизации/нормального состояния окон
- Сделать фабрику редакторов стилей
- Uml для архитектуры
- разделить полигон с заливкой цветом и текстурой
- улучшить MapObjectStyle.CompareDefenitionTags: 