1. Конвертирование osm в спец формат 
2. Выбор файла для отображения
3. Отображение карты со скроллом и масштабом.
4. Изменение настроек отображения для каждого из типов объекта osm (редактор тем). В редакторе показывать пример отображения
5. Выделение объекта на экране и просмотр доп. информации о нем
6. Изменение языка интерфейса
7. Поиск по тегам с пояснениями значений тегов
	Переход к объекту
8. При выходе запоминать открытую карту и положение области просмотра
9. Переход в определенную точку (lat,lon)
--------------------------------------------------------------------------
масштаб:
целой число - уровень масштаба

типы объектов
точка, линия, замкнутая линии, отношение

стиль объектов. Приоритет определения id объекта по стилю - по кол-ву тегов (п.1.1):
1. Общие настройки
	1.1 список тегов, обозначающих этот объект
	1.2 уникальный id этого типа объекта
	1.3 может ли объект быть точкой
	1.4 может ли объект быть линией
	1.5 может ли объект быть полигоном
	1.6 ключ тега из value которого будет отображать текстовая подпись объекта
2. стиль точки при каждом масштабе
	2.1 рисовать ли на этом уровне масштаба
	2.2 значок
3. стиль линии при каждом масштабе
	3.1 рисовать ли на этом уровне масштаба
	3.2 цвет
	3.3 толщина
	3.4 стиль (сполшная точка, тире, точка-тире)
4. стиль полигона при каждом масштабе
	4.1 рисовать ли на этом уровне масштаба
	4.2 цвет границы
	4.3 цвет заполнения
	[4.4] значок заполнения

---------------------------------------------------------------------------

1. Перевод XML во временные файлы
	1.1 Общие данные карты (<bounds .. />)
	1.2 Файл точек (node)
	1.3 Файл индексов точек (положений точки с данным id в файле)
	1.4 Файл линий (way)